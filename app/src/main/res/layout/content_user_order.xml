<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_user_order"
    tools:context=".UserActivity.Menu.OrderActivity">

    <ListView
        android:id="@+id/lv_user_order"
        android:layout_width="match_parent"
        android:layout_height="250dp"/>

    <TextView
        android:id="@+id/txt_total"
        android:layout_below="@+id/lv_user_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total : "
        android:layout_marginStart="30dp"
        android:layout_marginTop="20dp"
        android:textColor="#11C71B"
        />

    <TextView
        android:id="@+id/txt_hasil_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="totalnya"
        android:layout_below="@+id/lv_user_order"
        android:layout_toRightOf="@+id/txt_total"
        android:layout_marginTop="20dp"
        android:textColor="#000"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CheckOut"
        android:id="@+id/btn_checkout"
        android:layout_below="@+id/txt_total"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        />

</RelativeLayout>